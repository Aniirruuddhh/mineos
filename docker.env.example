# Local Docker Compose defaults. Copy to .env.docker only if you need to change them.
POSTGRES_DB=mineos
POSTGRES_USER=postgres
POSTGRES_PASSWORD=mineos_demo_password
POSTGRES_HOST_PORT=5433
AUTH_SESSION_SECRET=replace-with-a-long-random-secret
DEMO_LOGIN_PASSWORD=MineOSDemo2026!
